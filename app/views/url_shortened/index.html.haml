.col-md-6.col-md-offset-3
  .main-card
    %h2.text-center Short your links!
    %h4.text-center A simple URL shortener without database
    = form_for :url, html: { class: "text-center  form" }, url: 'url_shortened', method: :post do |f|
      .form-group
        = f.text_field :original_url, as: :string, placeholder: "Enter the URL you want to shorten...", label: false, class: 'form-control', required: true
      .form-group
        = f.submit "Shorten!", class: "btn btn-primary"

%br
%table.table.table-hover.table-bordered.table-striped
  %thead
    %tr
      %th Orignal Url
      %th Short Url
  %tbody
    -@urls.each do |key, value|
      %tr
        %td
          = value
        %td
          = short_url_link key
